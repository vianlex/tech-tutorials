---
title: 第三章 ES6+ 现代特性
linkTitle: ES6+ 特性
description: 解构赋值、模板字符串、展开运算符、模块与类
weight: 23
---

# ES6+ 现代特性

## 解构赋值 {#destructuring}

```javascript
// 数组解构
const [first, second, ...rest] = [1, 2, 3, 4, 5];
console.log(first);  // 1
console.log(rest);   // [3, 4, 5]

// 对象解构
const user = { name: 'Alice', age: 25 };
const { name, age } = user;
console.log(name);   // "Alice"

// 重命名 + 默认值
const { name: username, age: userAge = 18 } = user;
```

## 模板字符串 {#template-strings}

```javascript
const name = 'Alice';
const greeting = `Hello, ${name}!`;  // 反引号 + ${}

// 多行字符串
const html = `
  <div>
    <p>多行内容</p>
  </div>
`;
```

## 展开与剩余运算符 {#spread-rest}

```javascript
// 展开（spread）
const arr1 = [1, 2, 3];
const arr2 = [...arr1, 4, 5];     // [1,2,3,4,5]
const merged = { ...user, city: 'Beijing' };

// 剩余（rest）
function sum(...numbers) {
    return numbers.reduce((a, b) => a + b, 0);
}
sum(1, 2, 3, 4);  // 10
```

## 默认参数 {#default-params}

```javascript
function greet(name = 'World') {
    console.log(`Hello, ${name}`);
}
greet();         // "Hello, World"
greet('Alice');  // "Hello, Alice"
```

## 模块（ES Modules） {#modules}

```javascript
// math.js —— 导出
export function add(a, b) { return a + b; }
export default function multiply(a, b) { return a * b; }

// main.js —— 导入
import multiply, { add } from './math.js';
console.log(add(1, 2));        // 3
console.log(multiply(2, 3));   // 6
```

## 类（class） {#class}

```javascript
class Animal {
    constructor(name) {
        this.name = name;
    }

    speak() {
        console.log(`${this.name} makes a sound`);
    }
}

class Dog extends Animal {
    speak() {
        console.log(`${this.name} barks`);
    }
}

const dog = new Dog('Rex');
dog.speak();  // "Rex barks"
```

## 小结 {#summary}

ES6+ 让 JavaScript 更简洁、更接近现代编程语言的体验。下一章学习如何用 DOM 操作网页。
