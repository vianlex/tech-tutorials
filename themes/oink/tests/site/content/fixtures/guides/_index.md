---
title: Nested guides
description: A section fixture that proves tree-order navigation across levels.
weight: 35
# Page-level override of the site setting (this fixture site uses
# `params.ui.section_index: cards`): this section index stays a list. The
# page key is the site key without its `ui.` prefix.
section_index: list
---

The section index participates in the same sequence as its children.
