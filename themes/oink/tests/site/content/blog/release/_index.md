---
title: Releases
description: Every OINK release note, newest first, each one a part of the same series.
type: blog
# The compact table is this section's own default; the toolbar cycle (list,
# cards, table) is live here because the section uses the ordinary blog
# index. The dedicated `layout: releases` project-and-tag index remains a
# theme feature, pinned by its own fixture.
blog_index: table
# The whole section reads immersively: a hero opening drawn from the section
# image, no sidebar, and a clouds-free flow rail; the blog shell already
# renders no breadcrumb. The index carries the keys itself (a cascade never
# applies to the page declaring it) and passes the same recipe to every note
# below.
images: [/images/releasenote.webp]
featured_image: hero
sidebar_enabled: false
toc_taxonomies: false
cascade:
  images: [/images/releasenote.webp]
  featured_image: hero
  toc_style: flow
  toc_taxonomies: false
  sidebar_enabled: false
---

Real release notes, 0.1.0 through 0.6.0, as one compact table — the whole
section at once, cycle the toolbar control for rows or cards. Every note is
part of the [OINK releases](/series/oink-releases/) series.
