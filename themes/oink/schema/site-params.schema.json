{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "GENERATED by bin/generate-config-schema.py from hugo.yaml and check-params.py -- do not edit",
  "title": "OINK site configuration (hugo.yaml)",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "params": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "highlight_classes": {
          "description": "Highlight code with Chroma class names so it follows the theme's light/dark palettes. Hugo ignores `markup` set by a theme, so the theme applies this through layouts/_markup/render-codeblock.html instead. Set to false to fall back to Hugo's inline highlight styles.",
          "type": "boolean",
          "default": true
        },
        "offline_search_on_serve": {
          "description": "`hugo server` builds the local index and search runtime too, so a preview behaves like the deployed site. Set false to skip it on very large sites.",
          "type": "boolean",
          "default": true
        },
        "offline_search": {
          "description": "Local Lunr search. Off by default; a site opts in.",
          "type": "boolean",
          "default": false
        },
        "offline_search_index": {
          "type": "string",
          "default": "content"
        },
        "offline_search_summary_length": {
          "type": "integer",
          "default": 70
        },
        "offline_search_max_results": {
          "type": "integer",
          "default": 10
        },
        "time_format_blog": {
          "description": "ISO-8601, the one format every locale reads the same way. A weekday name is decoration a date line does not need.",
          "type": "string",
          "default": "2006-01-02"
        },
        "time_format_default": {
          "type": "string",
          "default": "2006-01-02"
        },
        "github_branch": {
          "description": "Branch used by the edit / history / new-page links derived from github_repo.",
          "type": "string",
          "default": "main"
        },
        "page_width": {
          "type": "string",
          "default": "normal"
        },
        "reading_width": {
          "description": "Inner reading measure for Book pages: slim | normal | wide. Unlike page_width, this changes content rather than the surrounding shell.",
          "type": "string",
          "default": "normal"
        },
        "markmap": {
          "description": "Diagram runtimes are off until a site opts in. `markmap: true` renders ```markmap fences; plantuml and drawio also require their server URLs.",
          "type": "boolean",
          "default": false
        },
        "plantuml": {
          "type": [
            "boolean",
            "object"
          ],
          "additionalProperties": true,
          "properties": {
            "enable": {
              "type": "boolean",
              "default": false
            },
            "svg": {
              "description": "Read by the theme's templates; no declared default."
            },
            "svg_image_url": {
              "description": "Read by the theme's templates; no declared default."
            }
          }
        },
        "drawio": {
          "type": [
            "boolean",
            "object"
          ],
          "additionalProperties": true,
          "properties": {
            "enable": {
              "type": "boolean",
              "default": false
            },
            "drawio_server": {
              "description": "Read by the theme's templates; no declared default."
            }
          }
        },
        "print": {
          "type": [
            "boolean",
            "object"
          ],
          "additionalProperties": true,
          "properties": {
            "toc": {
              "type": "boolean",
              "default": true
            },
            "section_break_wordcount": {
              "type": "integer",
              "default": 50
            }
          }
        },
        "comments": {
          "type": [
            "boolean",
            "object"
          ],
          "description": "`comments: false` is the boolean shorthand for this map.",
          "additionalProperties": true,
          "properties": {
            "enable": {
              "type": "boolean",
              "default": false
            },
            "type": {
              "type": "string",
              "default": "giscus"
            },
            "giscus": {
              "type": "object",
              "additionalProperties": true,
              "properties": {
                "mapping": {
                  "type": "string",
                  "default": "pathname"
                },
                "strict": {
                  "type": "integer",
                  "default": 0
                },
                "reactionsEnabled": {
                  "type": "integer",
                  "default": 1
                },
                "emitMetadata": {
                  "type": "integer",
                  "default": 0
                },
                "inputPosition": {
                  "type": "string",
                  "default": "top"
                },
                "theme": {
                  "type": "string",
                  "default": "auto"
                },
                "loading": {
                  "description": "lightTheme / darkTheme default to the theme's own giscus palettes (assets/css/giscus-{light,dark}.css, published only where comments render); set a giscus built-in name or a stylesheet URL to override.",
                  "type": "string",
                  "default": "lazy"
                }
              }
            }
          }
        },
        "ui": {
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "theme_color": {
              "description": "Optional accent recolor: a `#rgb`/`#rrggbb` hex tints the shell's accent grounds -- the selected sidebar row, hover washes, the outline pill and its travelling rail, tag hovers, text selection, focus rings, and each root's mark in the sidebar switcher. It deliberately does NOT touch links or inline code, which stay the brand palette so running text reads the same in every section. Front matter `theme_color` overrides it per page, and a section root's `cascade` gives a whole section its own identity. `theme_color_dark` carries the dark palette; when omitted it is derived by lightening the light color until it reads AA on the dark canvas. Blank keeps the brand navy, and an invalid value warns and keeps it too; front matter `theme_color: false` opts a page out of an inherited color entirely.",
              "type": "string",
              "default": ""
            },
            "theme_color_dark": {
              "type": "string",
              "default": ""
            },
            "navbar_enabled": {
              "description": "The site navbar and footer render on every layout by default. Rendering, auto-hide, and footer style can be overridden per page (or per section, via a front-matter cascade) with the matching top-level keys.",
              "type": "boolean",
              "default": true
            },
            "navbar_autohide": {
              "description": "Auto-hide never applies to the home page unless its own front matter asks.",
              "type": "boolean",
              "default": false
            },
            "footer_style": {
              "type": "string",
              "default": "fat"
            },
            "landing_search": {
              "description": "Non-home pages with `layout: landing` reuse the search/command palette when offline_search is enabled. Set false to omit it from that shell.",
              "type": "boolean",
              "default": true
            },
            "section_index": {
              "description": "Form of a branch page's child index: list or cards.",
              "type": "string",
              "default": "list"
            },
            "section_index_columns": {
              "type": "integer",
              "default": 2
            },
            "blog_index": {
              "description": "Form of the blog section list pages: list (rows), cards (a grid), or table (one compact row per post -- the whole section at once, no year groups, no pagination). Front matter `blog_index` (on the blog root, or through its cascade) overrides it per section.",
              "type": "string",
              "default": "list"
            },
            "blog_index_columns": {
              "type": "integer",
              "default": 3
            },
            "blog_index_toggle": {
              "description": "Let a reader cycle the index through list, cards, and table from the index toolbar. Off by default: it puts every form in the document. The hidden ones load no images, but their markup is real.",
              "type": "boolean",
              "default": false
            },
            "blog_index_size": {
              "description": "Posts per page on a list or cards blog index; the table form always shows everything. Hugo ignores `pagination` set by a theme, so the list template passes this to .Paginate itself. Twelve divides by two, three, and four, so no card row is left short.",
              "type": "integer",
              "default": 12
            },
            "lastmod_commit": {
              "type": "string",
              "default": "subject"
            },
            "annotation": {
              "description": "Page provenance / revision block at the end of a page. Front matter `annotation: false` turns it off for one page. Its upstream attribution line is driven by front matter (`upstream_link` plus `upstream_name`, `upstream_copyright`, `upstream_license`, `upstream_notice`); the theme declares no defaults for those because they are the site's own facts.",
              "type": "boolean",
              "default": true
            },
            "backlinks": {
              "description": "Static backlinks: a right-rail group beside the table of contents listing the pages that link here. Derived from ordinary Markdown links and ref/relref at build time; no runtime. Pages override with the front matter key `backlinks`.",
              "type": "boolean",
              "default": false
            },
            "translation_notice": {
              "description": "Language code of the authoritative version, so a translated page can say so and link to it; false disables the line. Front matter `translation_notice: false` opts out a page written natively in this language.",
              "type": "boolean",
              "default": false
            },
            "reading_time": {
              "description": "Reading time in the article header. Front matter `reading_time: false` hides it for one page.",
              "type": "boolean",
              "default": false
            },
            "image_zoom": {
              "description": "Native image preview. Front matter `image_zoom: true|false` overrides.",
              "type": "boolean",
              "default": false
            },
            "dark_mode": {
              "description": "Dark palette and theme menu: `dark_mode: true` is the shorthand for `dark_mode: { enable: true, show_menu: true }`.",
              "type": "boolean",
              "default": false
            },
            "typography": {
              "type": "string",
              "default": "technical"
            },
            "shell_types": {
              "type": "array",
              "default": [
                "docs",
                "book",
                "blog",
                "swagger"
              ]
            },
            "docs_section": {
              "type": "string",
              "default": "docs"
            },
            "blog_section": {
              "type": "string",
              "default": "blog"
            },
            "featured_image": {
              "description": "How a blog page presents the featured image `images` selects: none | banner (a framed figure above the title) | wash (the image behind the article header at a low opacity) | hero (the immersive presentation: the blog shell paints the image as its own full-bleed backdrop and moves the opening down — on single pages and section indexes alike). Front matter `featured_image` overrides it for one page or, in a cascade, one section. A page that carries no image renders nothing either way.",
              "type": "string",
              "default": "none"
            },
            "toc_style": {
              "description": "Presentation of the right rail: fixed (a panel pinned to the viewport) or flow (a wider panel in the content flow that starts where the article starts and pins only on scroll — the immersive blog rail).",
              "type": "string",
              "default": "fixed"
            },
            "toc_taxonomies": {
              "description": "Taxonomy term clouds on the right rail. Front matter `toc_taxonomies` (or a section cascade) turns them off; a rail left with neither a TOC nor clouds renders nothing at all.",
              "type": "boolean",
              "default": true
            },
            "docs_sidebar_root": {
              "description": "Root of the docs sidebar tree: section (the docs section itself) or home (the whole site).",
              "type": "string",
              "default": "section"
            },
            "sidebar_icon_policy": {
              "description": "Sidebar icon density: all | groups | none.",
              "type": "string",
              "default": "all"
            },
            "pager_types": {
              "description": "Sequential navigation is on by default for reading-oriented content. Override this list site-wide, or set `pager: false` in front matter.",
              "type": "array",
              "default": [
                "docs",
                "book",
                "blog"
              ]
            },
            "sidebar_headings": {
              "description": "Book pages may opt into an h2-h4 branch beneath the active sidebar row.",
              "type": "boolean",
              "default": false
            },
            "sidebar_cache_limit": {
              "description": "Cache section trees once a site reaches this many pages. Active state is restored client-side when the cached tree is used.",
              "type": "integer",
              "default": 500
            },
            "book_draft_banner": {
              "description": "Draft state is always labeled in the sidebar; this optional banner adds an explicit notice at the start of draft book pages.",
              "type": "boolean",
              "default": false
            },
            "share": {
              "description": "Page-end share bar: a list drawn from x | bluesky | mastodon | facebook | linkedin | reddit | hackernews | telegram | whatsapp | line | pinterest | weibo | chatgpt | claude | email | copy. Empty means no bar. Every entry is a plain link (or the local copy button), never a platform script or a share count. Discord publishes no share-intent URL, so `copy` stands in for it. Front matter `share: [x, copy]` replaces the list for one page and `share: false` turns its bar off; a section cascade scopes the bar to, say, the blog.",
              "type": "array",
              "default": []
            },
            "feedback": {
              "type": [
                "boolean",
                "object"
              ],
              "description": "One-click structured docs feedback. The theme calls the site's existing gtag integration when present; no endpoint or free-text form is used.",
              "additionalProperties": true,
              "properties": {
                "enable": {
                  "type": "boolean",
                  "default": false
                },
                "reasons": {
                  "type": "boolean",
                  "default": true
                }
              }
            },
            "sidebar_menu_compact": {
              "type": "boolean",
              "default": true
            },
            "sidebar_menu_foldable": {
              "type": "boolean",
              "default": true
            },
            "sidebar_menu_truncate": {
              "type": "integer",
              "default": 2000
            },
            "sidebar_root_enabled": {
              "type": "boolean",
              "default": true
            },
            "sidebar_root_menu": {
              "type": "boolean",
              "default": true
            },
            "sidebar_width_min": {
              "type": "integer",
              "default": 220
            },
            "sidebar_width_max": {
              "type": "integer",
              "default": 480
            },
            "sidebar_item_overflow": {
              "type": "string",
              "default": "ellipsis"
            },
            "page_context_menu": {
              "type": [
                "boolean",
                "object"
              ],
              "additionalProperties": true,
              "properties": {
                "enable": {
                  "type": "boolean",
                  "default": true
                },
                "assistant_links": {
                  "description": "Opt in before exposing the built-in ChatGPT and Claude actions. When a reader activates one, the full browser URL (including query and hash) is sent to that third party; the page body is not uploaded.",
                  "type": "boolean",
                  "default": false
                },
                "links": {
                  "description": "Optional external actions may use the placeholders {url}, {title}, and {markdown_url}. No custom links are enabled by default.",
                  "type": "array",
                  "default": []
                }
              }
            },
            "sidebar_expand_levels": {
              "type": "integer",
              "default": 2
            },
            "scroll_spy": {
              "description": "Bootstrap's scroll spy for the in-page outline. Off by default: the theme's own outline runtime already tracks the active heading.",
              "type": "boolean",
              "default": false
            },
            "code_copy": {
              "description": "Copy control on code blocks. `false` sets the site-wide default only; a fence that names `copy` explicitly still wins.",
              "type": "boolean",
              "default": true
            },
            "keyboard_nav": {
              "description": "Single-key keyboard navigation (WASD/arrow tree focus, j/k outline jumps, q/e paging, and shell/palette toggles). Set false to drop the runtime from the bundle; pages can also opt out via front matter or a section cascade.",
              "type": "boolean",
              "default": true
            },
            "alt_site": {
              "description": "Read by the theme's templates; no declared default."
            },
            "breadcrumb": {
              "description": "Read by the theme's templates; no declared default."
            },
            "command_palette": {
              "description": "Read by the theme's templates; no declared default."
            },
            "fonts": {
              "description": "Read by the theme's templates; no declared default."
            },
            "github_stars": {
              "description": "Read by the theme's templates; no declared default."
            },
            "quick_links": {
              "description": "Read by the theme's templates; no declared default."
            },
            "sidebar_enabled": {
              "description": "Read by the theme's templates; no declared default."
            },
            "taxonomy_icons": {
              "description": "Read by the theme's templates; no declared default."
            }
          }
        },
        "archived_version": {
          "description": "Read by the theme's templates; no declared default."
        },
        "author": {
          "description": "Read by the theme's templates; no declared default."
        },
        "copyright": {
          "description": "Read by the theme's templates; no declared default."
        },
        "description": {
          "description": "Read by the theme's templates; no declared default."
        },
        "footer_center_info": {
          "description": "Read by the theme's templates; no declared default."
        },
        "gcs_engine_id": {
          "description": "Read by the theme's templates; no declared default."
        },
        "github_project_repo": {
          "description": "Read by the theme's templates; no declared default."
        },
        "github_repo": {
          "description": "Read by the theme's templates; no declared default."
        },
        "github_subdir": {
          "description": "Read by the theme's templates; no declared default."
        },
        "images": {
          "description": "Read by the theme's templates; no declared default."
        },
        "logo": {
          "description": "Read by the theme's templates; no declared default."
        },
        "mermaid": {
          "description": "Read by the theme's templates; no declared default."
        },
        "path_base_for_github_subdir": {
          "description": "Read by the theme's templates; no declared default."
        },
        "search": {
          "description": "Read by the theme's templates; no declared default.",
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "algolia": {
              "description": "Read by the theme's templates; no declared default."
            }
          }
        },
        "taxonomy": {
          "description": "Read by the theme's templates; no declared default.",
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "page_header": {
              "description": "Read by the theme's templates; no declared default."
            }
          }
        },
        "upstream_modified": {
          "description": "Read by the theme's templates; no declared default."
        },
        "upstream_source": {
          "description": "Read by the theme's templates; no declared default."
        },
        "url_latest_version": {
          "description": "Read by the theme's templates; no declared default."
        },
        "version": {
          "description": "Read by the theme's templates; no declared default."
        },
        "version_menu": {
          "description": "Read by the theme's templates; no declared default."
        },
        "version_menu_pagelinks": {
          "description": "Read by the theme's templates; no declared default."
        },
        "versions": {
          "description": "Read by the theme's templates; no declared default."
        },
        "wordmark": {
          "description": "Read by the theme's templates; no declared default."
        }
      },
      "description": "OINK theme parameters."
    }
  }
}
