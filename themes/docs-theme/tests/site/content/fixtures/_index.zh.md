---
title: 测试样例
linkTitle: 测试样例
description: 一组窄页面，为检查脚本与输出金样本固定主题行为。
icon: fa-solid fa-flask
# This section is its own sidebar root: /docs/ is the flat component
# reference, and the fixture tree (including the nested guides) is separate.
sidebar_root_for: self
cascade:
  type: docs
---

这些页面不是文档。每一页只演练主题的一小块行为，好让 `bin/check-*.py`
与金样本有稳定的比对对象。它们不在 `/docs/` 下（那里是组件参考），
也不出现在站点导航里。
